
# 1. Zero-shot vs Few-shot

## Zero-shot

예시 없이 모델에게 작업을 지시하는 방식이다.

### 장점

- 가장 간단하다.
- 토큰 사용량이 적다.
- 대부분의 일반적인 작업에서 충분한 성능을 낸다.

### 단점

다음과 같은 경우에는 결과가 흔들릴 수 있다.

- 분류 기준이 모호한 경우
- 출력 형식이 중요한 경우

---

## Few-shot

예시를 함께 제공하여 모델이 기준을 학습하도록 하는 방식이다.

설명보다 실제 예시를 보여주는 것이 더 효과적이다.

### 장점

- 분류 기준이 명확해진다.
- 출력 형식 준수율이 높아진다.
- 결과의 일관성이 향상된다.

### Few-shot 설계 원칙

- 예시는 2~5개 정도가 적절하다.
- 경계선 사례를 포함한다.
- 라벨 분포를 균형 있게 구성한다.
- 출력 형식도 예시로 함께 제공한다.

### 주의사항

- 특정 라벨에 편향된 예시를 넣지 않는다.
- 평가셋과 예시를 중복하지 않는다.
- 너무 많은 예시는 비용만 증가시키고 효과는 크지 않다.

---

# 2. Chain of Thought (CoT)

## 개념

모델이 최종 답을 내기 전에 사고 과정을 단계적으로 작성하도록 하는 기법이다.

```text
문제

↓

변수 정리

↓

계산 과정

↓

최종 답
```

---

## 장점

- 계산 정확도가 향상된다.
- 논리적 추론이 쉬워진다.
- 오류가 발생한 위치를 확인할 수 있다.
- 검증이 가능하다.

---

## 적합한 작업

- 계산
- 논리 문제
- 복잡한 추론
- 단계별 분석

---

# 3. Self-Consistency

## 개념

같은 문제를 여러 번 독립적으로 해결한 후 가장 많이 나온 답을 선택하는 방법이다.

```text
질문

↓

모델 실행 × N

↓

다수결

↓

최종 답
```

---

## 특징

- 비결정성을 활용한다.
- 정확도를 높일 수 있다.
- 실행 비용은 실행 횟수만큼 증가한다.

---

## 적합한 작업

- 금융 계산
- 규정 해석
- 정확성이 중요한 업무

---

# 4. ReAct (Reasoning + Acting)

## 개념

생각(Reasoning)과 행동(Action)을 반복하며 문제를 해결하는 방식이다.

AI Agent의 핵심 동작 원리이다.

```text
Thought

↓

Action

↓

Observation

↓

Thought

↓

Answer
```

---

## 동작 과정

### Thought

현재 필요한 정보를 판단한다.

### Action

필요한 도구(API, 검색 등)를 호출한다.

### Observation

도구 결과를 확인한다.

### Answer

관찰한 결과를 기반으로 최종 답변을 생성한다.

---

## 장점

- 외부 데이터를 활용할 수 있다.
- 환각(Hallucination)을 줄일 수 있다.
- 실제 업무 자동화에 적합하다.

---

## 핵심 원칙

모르는 것은 추측하지 않고 반드시 도구를 통해 확인한다.

---

# 5. Tree of Thought (ToT)

## 개념

하나의 사고 흐름이 아닌 여러 가지 해결 방법을 탐색한 후 가장 적합한 방법을 선택하는 기법이다.

```text
문제

├── 방법 A
├── 방법 B
└── 방법 C

↓

비교

↓

최적안 선택
```

---

## 특징

- 다양한 접근 방식을 비교할 수 있다.
- 정답이 없는 문제에 적합하다.

---

## 활용 분야

- 전략 수립
- 시스템 설계
- 기획
- 아이디어 탐색

---

# 6. 프롬프트 기법 선택 기준

| 상황 | 추천 기법 |
|------|-----------|
| 단순 작업 | Zero-shot |
| 기준이 모호한 분류 | Few-shot |
| 계산 및 논리 | CoT |
| 외부 정보 필요 | ReAct |
| 다양한 해결책 탐색 | Tree of Thought |

---

# 7. 시스템 프롬프트와 사용자 프롬프트

## 시스템 프롬프트(System Prompt)

항상 유지되는 규칙을 정의하는 프롬프트이다.

### 포함 내용

- 역할(Role)
- 규칙
- 출력 형식
- 금지사항
- 판단 기준

---

## 사용자 프롬프트(User Prompt)

매 요청마다 변경되는 실제 입력 데이터이다.

### 포함 내용

- 사용자 질문
- 입력 데이터
- 처리 대상 정보

---

## 역할 비교

| 시스템 프롬프트 | 사용자 프롬프트 |
|----------------|----------------|
| 변하지 않는 규칙 | 매번 변경되는 입력 |
| 역할 정의 | 데이터 전달 |
| 출력 형식 정의 | 실제 요청 |

---

## 설계 원칙

```text
System Prompt → 규칙

User Prompt → 데이터
```

자동화에서는 시스템 프롬프트와 사용자 프롬프트를 분리하는 것이 중요하다.

---

# 실습 - 음식 배달 리뷰 분류 시스템

이번 실습에서는 음식 배달 리뷰를 자동으로 분석하는 AI를 설계하였다.

시스템 프롬프트를 이용하여 AI의 역할과 규칙을 정의하고, 항상 동일한 JSON 형식으로 결과를 출력하도록 구성하였다.

## 시스템 프롬프트

```text
너는 음식 배달 서비스의 리뷰를 분리하는 CX팀의 10년차 직원이야.

# 임무
입력된 배달 리뷰 1건을 분석해 지정된 JSON 형식으로만 응답한다.

# 분류 기준
- category: 한식 / 중식 / 양식 / 일식 / 기타 중 하나
- sentiment: 긍정 / 부정
  - 긍정: 만족, 칭찬, 재주문 의사
  - 부정: 불만, 항의, 개선 요청
- rating: 별점 1 ~ 5점
  - 5: 매우 만족, 강한 칭찬, 재주문 의사 표현
  - 4: 전반적으로 만족, 사소한 아쉬움
  - 3: 장단점이 비슷하거나 특별한 감정 없음
  - 2: 불만이 있으나 일부는 괜찮음
  - 1: 매우 불만, 강한 불평, 환불·재주문 거부 수준
- summary: 30자 이내 요약

# 규칙
- JSON 외의 텍스트를 절대 출력하지 않는다.
- rating은 리뷰의 전체적인 만족도를 기준으로 판단한다.
- 리뷰에 없는 내용을 추측하지 않는다.
- summary는 핵심 내용만 간결하게 작성한다.

# 출력 예시
{
  "category":"한식",
  "sentiment":"긍정",
  "rating":5,
  "summary":"음식이 맛있고 재주문 의사 있음"
}
```

---

## 시스템 프롬프트 설계 포인트

### 1. 역할(Role) 정의

AI에게 **음식 배달 서비스 CX팀의 10년차 직원**이라는 역할을 부여하여 리뷰를 고객센터 담당자의 관점에서 분석하도록 하였다.

---

### 2. 명확한 임무(Task) 정의

AI의 역할을 리뷰 분석으로 한정하고, JSON 형식으로만 응답하도록 지정하였다.

---

### 3. 세부 분류 기준 정의

리뷰를 다음 기준으로 분석하도록 정의하였다.

- 음식 종류(category)
- 감성(sentiment)
- 만족도(rating)
- 요약(summary)

특히 rating은 각 점수의 의미를 함께 정의하여 일관된 판단이 가능하도록 하였다.

---

### 4. 출력 형식 고정

JSON만 출력하도록 규칙을 지정하였다.

예시

```json
{
  "category": "한식",
  "sentiment": "긍정",
  "rating": 5,
  "summary": "음식이 맛있고 재주문 의사 있음"
}
```

---

### 5. Guardrail(제약 조건)

모델의 불필요한 추측이나 출력 오류를 방지하기 위해 다음과 같은 규칙을 정의하였다.

- JSON 외 출력 금지
- 리뷰에 없는 내용 추측 금지
- 전체 만족도를 기준으로 rating 판단
- summary는 핵심만 간결하게 작성

---

## 실습을 통해 배운 점

- 시스템 프롬프트는 AI의 역할과 규칙을 정의한다.
- 판단 기준을 구체적으로 작성할수록 응답의 일관성이 높아진다.
- JSON 출력 형식을 고정하면 자동화 시스템에서 활용하기 쉽다.
- Guardrail을 통해 환각(Hallucination)을 줄이고 출력 품질을 높일 수 있다.

---

# 8. JSON 출력 안정화 기법

## 1. 출력 예시 제공

스키마를 설명하는 것보다 실제 JSON 예시를 제공하는 것이 더 효과적이다.

---

## 2. JSON 외 출력 금지

다음과 같이 명시하여 불필요한 텍스트 생성을 방지한다.

```text
JSON 외의 텍스트를 출력하지 않는다.
```

---

## 3. 예외 처리 정의

분류가 불가능한 경우의 출력 형식도 미리 정의한다.

예시

```json
{
  "category": "기타"
}
```

---

# 9. 프롬프트 평가

프롬프트는 감이 아니라 평가를 통해 개선해야 한다.

## 평가 절차

```text
평가셋 작성

↓

프롬프트 실행

↓

결과 비교

↓

실패 원인 분석

↓

프롬프트 수정

↓

재평가
```

---

## 평가 방법

### 정확 일치(Exact Match)

출력 결과와 정답이 동일한지 비교한다.

적합한 작업

- 분류
- 정보 추출

---

### 체크리스트 평가

형식과 규칙 준수 여부를 평가한다.

예시

- JSON 형식 준수
- 금지어 포함 여부
- 필수 항목 존재 여부

---

### LLM-as-Judge

다른 LLM을 심판으로 활용하여 결과를 평가하는 방법이다.

대량 평가에 적합하지만 사람의 기준과 비교하여 신뢰성을 검증해야 한다.

---

# 10. 프롬프트 개선 원칙

한 번에 하나의 요소만 수정해야 한다.

```text
평가

↓

실패 사례 확인

↓

원인 분석

↓

프롬프트 한 가지 수정

↓

재평가

↓

성능 비교
```

여러 요소를 동시에 수정하면 어떤 변경이 효과를 냈는지 판단하기 어렵다.

---